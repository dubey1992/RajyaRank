name: Staging Ops Task

# Manual, one-off operational tasks against the Staging database that need
# the full workspace (ts-node, workspace packages) rather than the stripped
# production image. Builds the API Dockerfile's builder stage (not the slim
# runner) and runs one of two fixed operations as a one-off Fargate task, in
# the same public subnet + app security group the migrate step already
# uses. Deliberately NOT a free-form command input — this task's execution
# role can read Staging's secrets, so the set of things it can run is fixed
# and reviewed here rather than caller-supplied.

on:
  workflow_dispatch:
    inputs:
      operation:
        description: 'Which fixed operation to run'
        required: true
        type: choice
        options:
          - seed-reference-data
          - bootstrap-super-admin

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: ap-south-1
  ECR_REPOSITORY: 485141928074.dkr.ecr.ap-south-1.amazonaws.com/rajyarank-api-staging
  ECS_CLUSTER: rajyarank-staging

jobs:
  ops:
    name: Build ops image and run task
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - uses: actions/checkout@v4

      - name: Assume the staging deploy role (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::485141928074:role/rajyarank-github-staging-deploy-role
          aws-region: ${{ env.AWS_REGION }}

      - name: Log in to ECR
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push the builder-stage (full workspace) image
        run: |
          docker build -f infra/docker/Dockerfile.api --target builder -t "$ECR_REPOSITORY:ops-latest" .
          docker push "$ECR_REPOSITORY:ops-latest"

      - name: Resolve the fixed command for the chosen operation
        id: op
        run: |
          case "${{ inputs.operation }}" in
            seed-reference-data)
              echo 'command=["sh","-c","cd /app && pnpm --filter @rajyarank/api prisma db seed"]' >> "$GITHUB_OUTPUT"
              ;;
            bootstrap-super-admin)
              echo 'command=["node","apps/api/scripts/bootstrap-super-admin.js"]' >> "$GITHUB_OUTPUT"
              ;;
            *)
              echo "::error::Unknown operation ${{ inputs.operation }}"
              exit 1
              ;;
          esac

      - name: Register ops task definition
        run: |
          cat > ops-task-def.json <<EOF
          {
            "family": "rajyarank-staging-ops",
            "networkMode": "awsvpc",
            "requiresCompatibilities": ["FARGATE"],
            "cpu": "1024",
            "memory": "2048",
            "executionRoleArn": "arn:aws:iam::485141928074:role/rajyarank-staging-ecs-execution-role",
            "taskRoleArn": "arn:aws:iam::485141928074:role/rajyarank-staging-task-role",
            "containerDefinitions": [{
              "name": "ops",
              "image": "$ECR_REPOSITORY:ops-latest",
              "workingDirectory": "/app",
              "essential": true,
              "command": ${{ steps.op.outputs.command }},
              "environment": [
                {"name": "NODE_ENV", "value": "production"},
                {"name": "APP_ENV", "value": "staging"},
                {"name": "BOOTSTRAP_ADMIN_NAME", "value": "${{ vars.BOOTSTRAP_ADMIN_NAME }}"}
              ],
              "secrets": [
                {"name": "DATABASE_URL", "valueFrom": "arn:aws:secretsmanager:ap-south-1:485141928074:secret:rajyarank/staging/database-url-RDD9QN"},
                {"name": "FIELD_ENCRYPTION_KEY", "valueFrom": "arn:aws:secretsmanager:ap-south-1:485141928074:secret:rajyarank/staging/field-encryption-key-5OP7hM"},
                {"name": "BOOTSTRAP_ADMIN_EMAIL", "valueFrom": "arn:aws:secretsmanager:ap-south-1:485141928074:secret:rajyarank/staging/bootstrap-admin-email"},
                {"name": "BOOTSTRAP_ADMIN_PASSWORD", "valueFrom": "arn:aws:secretsmanager:ap-south-1:485141928074:secret:rajyarank/staging/bootstrap-admin-password"}
              ],
              "logConfiguration": {
                "logDriver": "awslogs",
                "options": {
                  "awslogs-group": "/ecs/rajyarank-staging",
                  "awslogs-region": "ap-south-1",
                  "awslogs-stream-prefix": "ops"
                }
              }
            }]
          }
          EOF
          aws ecs register-task-definition --cli-input-json file://ops-task-def.json --query 'taskDefinition.revision' --output text

      - name: Run the ops task
        run: |
          TASK_ARN=$(aws ecs run-task \
            --cluster "$ECS_CLUSTER" \
            --task-definition rajyarank-staging-ops \
            --launch-type FARGATE \
            --network-configuration "awsvpcConfiguration={subnets=[subnet-08690545b2df36758,subnet-0fc7d3cb0cdb81126,subnet-009c75d2c0c3a100d],securityGroups=[sg-0e393600ccc73570e],assignPublicIp=ENABLED}" \
            --query 'tasks[0].taskArn' --output text)
          echo "Ops task: $TASK_ARN"
          aws ecs wait tasks-stopped --cluster "$ECS_CLUSTER" --tasks "$TASK_ARN"
          EXIT_CODE=$(aws ecs describe-tasks --cluster "$ECS_CLUSTER" --tasks "$TASK_ARN" --query 'tasks[0].containers[0].exitCode' --output text)
          echo "Exit code: $EXIT_CODE"
          if [ "$EXIT_CODE" != "0" ]; then
            echo "::error::Ops task ($OPERATION) failed with exit code $EXIT_CODE — check CloudWatch Logs (/ecs/rajyarank-staging, stream prefix ops)"
            exit 1
          fi
        env:
          OPERATION: ${{ inputs.operation }}
